1. Docs! Docs! Lotsa work on the docs.
2. Do the equilibrium Spectra
3. MemoryError for large N:
    Use python "shelve" module to store latest states (indexed by mu, alpha) to disk (after mpi_gather)
    Load and restart